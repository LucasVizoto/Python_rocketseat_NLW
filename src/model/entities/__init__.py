#quando rodamos o python3, a primeira coisa que ele busca são os inits
# então basicamente, isso de colocar __init__ em todas as pastas
# é para sinalizar que elas existem

#mostrando que essas classes existem para a FK funcioanr
from .eventos import Eventos
from .inscritos import Inscritos